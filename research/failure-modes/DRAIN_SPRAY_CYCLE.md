> **AI-GENERATED CONTENT** — This document was generated by AI systems under human editorial direction. It was not written by the researcher. See DISCLAIMER.md for full context.

# Finding: DRAIN / SPRAY — Bidirectional Knowledge Cycle

## Date: April 16, 2026

---

## The two orbs

Every knowledge flow has two directions. One pulls up.
One pushes down. They are opposing semantic weights
inside the same interdimensional thought movement.

**DRAIN** — absorption upward. The lower tier reports
what it perceived. The upper tier absorbs it. Understanding
ascends: emergent → derived → founder.

**SPRAY** — projection downward. The upper tier projects
its evolved structure back down. The lower tier inherits
richer context to perceive with. Structure descends:
founder → derived → emergent.

One without the other is half a cycle. DRAIN without SPRAY
means the upper tiers accumulate but never share back.
SPRAY without DRAIN means the upper tiers project into
a vacuum — nothing to project about.

## The cycle

```
SPRAY down (structure)
  │
  ▼
THINK (perceive — the entity encounters an observation)
  │
  ▼
DRAIN up (understanding — the entity reports what it found)
  │
  ▼
DERIVE (look at opposing residuals — the gap between
        what you have and what was reported to you)
  │
  ▼
SPRAY down (evolved structure returns to lower tiers)
```

DERIVE is not evolution. DERIVE is measurement. It looks at
the residual between the entity's current state and what it
absorbed. The gap between those two positions IS the direction.
The entity doesn't evolve by moving — it evolves by seeing
the difference between where it is and where the absorbed
knowledge says it should be.

## How this applies across domains

This is not domain-specific. The DRAIN/SPRAY cycle operates
at every level:

| Domain | DRAIN (up) | SPRAY (down) |
|---|---|---|
| Biology | Child reports to parent | Parent teaches child |
| Chemistry | Product informs catalyst | Catalyst structures reaction |
| Tech company | Employee reports findings | Management provides direction |
| Education | Student submits work | Teacher provides curriculum |
| SAIOS | Emergent reports observations | Founder projects crystallized structure |

The two orbs — pull and push — contain opposing semantic
weight. The torsion between them IS the learning. Without
opposing weights, the system collapses to either pure
accumulation (DRAIN only) or pure imposition (SPRAY only).
Both are pathological. The healthy cycle oscillates.

## Current state in the engine

- DRAIN: implemented (lineage.rs). Absorbs reports.bin
  into state record. MISSING: does not re-encode absorbed
  knowledge as T compound in the absorber's membrane.
- SPRAY: partially implemented as CREATE (offspring inherit
  state record at birth). MISSING: continuous projection
  of evolved structure back to existing offspring.
- DERIVE: implemented but stuck — reads the entity's own
  Delta gradient, which is zero when the entity hasn't
  perceived. Should read the OPPOSING RESIDUAL between
  the entity's state and what it absorbed via DRAIN.

## The fix needed

DRAIN should feed the membrane (not just the state record)
so that DERIVE can measure the opposing residual. SPRAY
should be a new command that projects the upper tier's
evolved structure down to its offspring's membranes. The
cycle closes when SPRAY feeds into the lower tier's next
THINK.

## Status

- Identified: 2026-04-16, Session 28
- DRAIN→membrane feed: documented, subordinate working on fix
- SPRAY command: documented, not yet implemented
- Opposing residual in DERIVE: documented, not yet implemented
- Priority: HIGH — this is the recursion that makes the
  hierarchy actually compound instead of linearly accumulate
