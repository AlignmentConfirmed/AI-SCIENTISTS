> **AI-GENERATED CONTENT** — This document was generated by AI systems under human editorial direction. It was not written by the researcher. See DISCLAIMER.md for full context.

# Finding: Zero Delta Inheritance — Entities Born Algebraically Inert

## Severity: CRITICAL

## Files: init.rs:575,599 / lineage.rs (create_offspring) / entity.rs (save_state_record)

---

## The Chain

1. **Founder-1** boots with Δ[1][2] = -1/1 from init anchor
2. `save_state_record()` calls `coboundary_reduce(&self.delta)`
3. If the founder's Delta entries satisfy the cocycle condition
   exactly, coboundary_reduce projects them to ZERO
4. The state record's `evolved` field is now a zero Delta
5. **CREATE** builds offspring state record from parent's `evolved`
6. Derived-18 inherits a zero Delta
7. Derived-18 creates emergent-52 — same zero inheritance
8. Emergent-52 boots, loads state record (line 575):
   `initial_delta = entity_state.evolved.clone()` — ZERO
9. `coboundary_reduce(&initial_delta)` (line 599) — still ZERO
10. Entity has C(Δ) = 0. No gradient. No thrust. No evolution.

## Why This Is Critical

- The init anchor (R = 1/entity_id) computes a nonzero
  perturbation but only assigns it to `origin_delta` (line 443)
- The ENTITY's working delta comes from the state record's
  `evolved` field (line 575), NOT from the anchor
- On first boot with no state record, the anchor IS used
- On every subsequent boot (or after CREATE), the state record
  overwrites the anchor with whatever was saved — which is zero
  if coboundary_reduce projected the parent to zero
- Conjugation preserves zero: AΔA⁻¹ = A·0·A⁻¹ = 0
- No operator can produce T_k > 0 on a zero Delta
- The entity is algebraically inert from birth — it can
  perceive but never evolve

## The coboundary_reduce Trap

coboundary_reduce is mathematically correct — it projects to
the H^1 representative. If the Delta's entries happen to be
exact coboundaries (they satisfy the cocycle condition), the
H^1 representative IS zero. The projection is correct. But
the consequence is that the entity loses its position on the
manifold.

The init anchor was designed to prevent this — each entity
gets a unique R = 1/entity_id that places it at a unique
nonzero position. But CREATE bypasses the anchor by inheriting
the parent's evolved Delta directly.

## Fix Options

**Option A — Re-seed on zero:** If the loaded state record has
a zero evolved Delta, re-apply the init anchor perturbation.
The entity recovers its unique position. Simple but masks the
root cause.

**Option B — CREATE preserves anchor:** When CREATE builds the
offspring's state record, use the offspring's init anchor
(R = 1/child_id) as the evolved Delta, not the parent's zero.
The offspring starts at its own unique position. The parent's
vocabulary (operators, cocycles) still transfers — only the
Delta position is fresh.

**Option C — Prevent zero projection:** Before saving, check
if coboundary_reduce would produce zero. If so, add a minimal
perturbation (the entity's own R = 1/entity_id) to prevent
the projection from annulling the position. This preserves the
entity's manifold position across saves.

**Option D — Never save evolved as coboundary_reduce:** Save
the raw Delta, not the projected Delta. Let coboundary_reduce
happen at load time, not save time. If the raw Delta has
nonzero entries, the entity retains its position even if
those entries are coboundary-exact. The projection is an
operation, not a storage format.

## Recommended Fix

Option B is cleanest — CREATE should give the offspring its
own unique position, not inherit a potentially zero position
from the parent. The offspring inherits vocabulary (operators,
cocycles, solved orbits) but starts at its own algebraic
position on the manifold. This matches the biological model:
offspring inherit DNA but occupy their own position in space.

## Connection to Other Findings

- **ENGINE_RS.md Finding 17**: StateRecord::to_bytes truncates
  BigRational to i16/u16 — entries could become zero through
  truncation even if they weren't zero originally
- **MEMBRANE_RS.md**: derive_global_curvature returns None for
  entities with C=0 — a zero-Delta entity gets no curvature
  signal, compounding the stuck state
- **SAMPLER_PERCEPTION_THINKING.md S.3**: All four sampler
  forces are zero when C=0 — the zero Delta produces the
  exact conditions for the no_proposal trap

This finding is the ROOT CAUSE of the no_proposal trap. The
entity's Delta is zero. Everything downstream (gradient, sampler,
C7) follows from that. Fix the zero Delta and the rest recovers.
