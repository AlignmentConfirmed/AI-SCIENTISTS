> **AI-GENERATED CONTENT** — This document was generated by AI systems under human editorial direction. It was not written by the researcher. See DISCLAIMER.md for full context.

# CCL Vocabulary Audit — Residual Inconsistencies

## Date: April 16, 2026

## Classification: CCL Compliance / Codebase Audit

---

## Methodology

Every `.rs` source file in `/opt/repos-code/convergence-engine/src/` was scanned
against the CCL specification (CCL.md Parts I-V). Each finding is above the
sovereignty boundary — not Rust stdlib, not OS-level, not legacy path strings.

Reference: `/opt/repos-code/convergence-engine/CCL.md`

---

## Category 1 — Entity Hierarchy (CCL Part IIb)

### 1.1 "witness" — should be "entity" or tier-specific name

| File | Line | Context | Severity |
|------|------|---------|----------|
| `chain/temporal_ledger.rs` | 13 | `Σ_maj witness weight > 0.5` | Comment |
| `chain/temporal_ledger.rs` | 78 | `is finalized (witness weight > 0.5)` | Comment |
| `chain/temporal_ledger.rs` | 136 | `Record a witness attestation` | Comment |
| `chain/temporal_ledger.rs` | 244-266 | `witness` as loop variable and function `add_witness()` | Code + API |
| `perception/mod.rs` | 741 | `The witness rotates its local frame` | Comment |
| `thinking/mod.rs` | 200 | `Same structure as the witness` | Comment |
| `thinking/peel_legacy.rs` | 19 | `witness governance` | Comment |
| `thinking/peel_legacy.rs` | 286 | `witness reads its own metabolic state` | Comment |
| `color.rs` | 547 | `The witness never left` | Comment |
| `color.rs` | 1294 | `every orb is ultimately part of the Witness` | Comment |
| `bin/main/observation.rs` | 100 | `"STATUS" \| "WITNESS"` command dispatch | Code |
| `bin/main/observation.rs` | 273,364 | `Self-witness`, `we witness the peer` | Comment |

**Note:** `LAST_WITNESS` protocol name retained per CCL decision (entity.rs:313).

### 1.2 "primary entity" / "primary entities" — should be "entity" (tier-agnostic)

59 instances across 8 files. CCL Part IIb: "The struct is named for what they all
are: entities." Using "primary entity" implies tier-specificity where none exists.

| File | Count | Examples |
|------|-------|---------|
| `membrane.rs` | 36 | "primary entities", "primary entity's T", "all primary entities agree" |
| `perception/mod.rs` | 11 | "primary entity's escalation", "primary entity perceives", "primary entity's genomic aperture" |
| `thinking/mod.rs` | 4 | "primary entity's", "primary entities" |
| `perception/senses/vision.rs` | 2 | "primary entity's" |
| `bin/main/communication.rs` | 2 | "primary entities" |
| `bin/main/cognition.rs` | 2 | "primary entity" |
| `lineage.rs` | 1 | "primary entity" |
| `bin/main/observation.rs` | 1 | "primary entity" |

---

## Category 2 — Data Lexicon (CCL Part III)

### 2.1 "training" — should be "practice"

The module `training.rs` exists and is referenced throughout. CCL Part III:
"Entities practice. They are not trained. Training implies an external trainer."

| File | Line | Context |
|------|------|---------|
| `lib.rs` | 24 | `pub mod training;` — module name |
| `training.rs` | entire | Module file — struct `TrainingRecord`, `SeedClass`, etc. |
| `mdc.rs` | 33 | `use crate::training::TrainingRecord;` |
| `sampler.rs` | 33 | `use crate::training::{TrainingRecord, SeedClass, RECORD_SIZE};` |
| `perception/mod.rs` | 1006,1384,1687,1921,1923,1935,2298,2439,2489,2513,2872,2876,2908,3151,3157,3175,3330,3604 | "training pairs", "training consensus", "training examples" — 18+ instances |

**Scope:** This is a module-level rename. `TrainingRecord` → `PracticeRecord`,
`training.rs` → `practice.rs`, all "training pairs" → "practice pairs".

### 2.2 "corrupted" / "CORRUPTED" — should be "fractured" / "FRACTURED"

| File | Line | Context |
|------|------|---------|
| `engine.rs` | 49 | `Triggers CORRUPTED sluice state` |
| `engine.rs` | 2840 | `Failure → CORRUPTED (hardware fault)` |
| `engine.rs` | 3015 | `Failure → CORRUPTED` |
| `agent.rs` | 224 | `If it is corrupted` |
| `ledger.rs` | 532 | `full rebuild on CORRUPTED` |

### 2.3 "consumed" — should be "absorbed"

| File | Line | Context |
|------|------|---------|
| `bin/main/cognition.rs` | 1145 | `Truncate consumed queue` |
| `bin/main/lineage.rs` | 330 | `Truncate consumed queue` |

### 2.4 "genome" — superseded by "state record"

| File | Line | Context |
|------|------|---------|
| `harmonic_tuning.rs` | 82 | `prevent genome bloat` |

### 2.5 "genomic aperture" — in perception comments

| File | Line | Context |
|------|------|---------|
| `perception/mod.rs` | 490 | `primary entity's genomic aperture` |

---

## Category 3 — Authority Lexicon (CCL Part I)

### 3.1 "breed" / "breeding" — no CCL replacement defined, but biological

| File | Line | Context |
|------|------|---------|
| `crucible.rs` | 18 | `cross-breed offspring` |
| `crucible.rs` | 144 | `cannot breed` |

### 3.2 "dead-ends" — should use CCL cessation lineage

| File | Line | Context |
|------|------|---------|
| `bin/main/cognition.rs` | 465 | `knowledge dead-ends at the derived tier` |
| `bin/main/lineage.rs` | 391 | `knowledge dead-ends` |

### 3.3 "force" (verb on entity behavior)

| File | Line | Context |
|------|------|---------|
| `bin/main/init.rs` | 784 | `EJECT_AGENT — force sluice to GATED` |

CCL Part III: "A projection is applied. Not forced."

---

## Category 4 — Structural (Module-Level)

### 4.1 `training.rs` module

The entire module is named `training`. Contains `TrainingRecord`, `SeedClass`,
`FailureCode`. Used by `mdc.rs` and `sampler.rs`. CCL says "practice".

### 4.2 `GENE` magic bytes in state record serialization

| File | Line | Context |
|------|------|---------|
| `engine.rs` | 1309 | `[GENE magic: 4 bytes]` — comment references old magic |

The comment describes a magic header `0x47454E45` ("GENE") that doesn't appear
in the actual `to_bytes()` implementation. The serialization format starts with
`process_class` directly. The comment is stale.

### 4.3 `add_witness()` API in temporal_ledger.rs

| File | Line | Context |
|------|------|---------|
| `chain/temporal_ledger.rs` | 136+ | Function name `add_witness()`, parameter `witness` |

This is an API surface — renaming affects all callers. The function records
timekeeper attestations. CCL: timekeepers observe, they don't witness.

---

## Summary

| Category | Count | Severity |
|----------|-------|----------|
| "witness" (code + comments) | 14 | High — includes API names |
| "primary entity/entities" (comments) | 59 | Medium — comments only |
| "training" (module + all references) | 20+ | High — module-level rename |
| "corrupted" (comments) | 5 | Low — comment only |
| "consumed" (comments) | 2 | Low — comment only |
| "genome" (comments) | 2 | Low — comment only |
| "breed" (comments) | 2 | Low — comment only |
| "dead-ends" (comments) | 2 | Low — comment only |
| "force" (comment) | 1 | Low — comment only |
| "GENE" magic (stale comment) | 1 | Low — stale documentation |

**Total: ~108 inconsistencies above the sovereignty boundary.**

The highest-severity items are:
1. `training.rs` module and `TrainingRecord` struct — module-level rename to `practice`
2. `add_witness()` API in `temporal_ledger.rs` — function rename
3. `"WITNESS"` command in `observation.rs` dispatch — protocol-level

---

## Recommendation

The 59 "primary entity" instances are the largest volume. They should be "entity"
(tier-agnostic, per CCL Part IIb). The `training` module rename is the highest
structural impact — it touches imports across 4 files and the struct name appears
in 5+ files.

No logic changes required. All findings are vocabulary layer (names and comments).
The mathematical invariants are unaffected.
